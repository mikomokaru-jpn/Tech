wordArray.forEach(function(value) {    let element = document.createElement('p');    let text = record['text'];    text = text.replace(/\r?\n/g, '<br>'); //改行コードの変換     //変数（value）を正規表現のパターンに指定する方法    text = text.replace(new RegExp(value,"g"),                         '<span class="match">'+value+'</span>');});                element.innerHTML = text;  //element.textContent = text; //（誤）タグを評価しないlistTop.appendChild(element);                    